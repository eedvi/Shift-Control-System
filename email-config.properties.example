# Email Configuration for Shift Control System
# Configure these settings to enable real email delivery

# SMTP Server Settings
smtp.host=smtp.gmail.com
smtp.port=587
smtp.use.ssl=false
smtp.use.starttls=true
# Email Credentials (IMPORTANT: Use App Passwords for Gmail)
email.username=
email.password=
email.from=

# Enable/Disable Email Service
email.enabled=false

# Instructions for Gmail Setup:
# 1. Enable 2-Factor Authentication on your Google account
# 2. Generate an App Password: https://myaccount.google.com/apppasswords
# 3. Use the App Password (not your regular password) in email.password
# 4. Set email.enabled=true to activate real email sending
#
# For other email providers, update the SMTP settings accordingly:
# - Outlook: smtp-mail.outlook.com, port 587
# - Yahoo: smtp.mail.yahoo.com, port 587
# - Custom SMTP: Use your provider's settings
